<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title | decodeHtml }} - {% endif %}{{ site.name }}</title>
  <meta name="description" content="{{ description or site.description }}">

  {# Favicon #}
  <link rel="icon" type="image/png" sizes="32x32" href="/wp-content/uploads/2016/10/Think-By-Numbers-tbn-logo-favicon-icon-Thumbnail-600-x-600-transparent-32x32.png">
  <link rel="icon" type="image/png" sizes="192x192" href="/wp-content/uploads/2016/10/cropped-Think-By-Numbers-tbn-logo-favicon-icon-Thumbnail-600-x-600-transparent-192x192.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/wp-content/uploads/2016/10/cropped-Think-By-Numbers-tbn-logo-favicon-icon-Thumbnail-600-x-600-transparent-180x180.png">

  {# Open Graph / Facebook #}
  <meta property="og:locale" content="en_US">
  <meta property="og:type" content="{% if title %}article{% else %}website{% endif %}">
  <meta property="og:title" content="{% if title %}{{ title | decodeHtml }}{% else %}{{ site.name }}{% endif %}">
  <meta property="og:description" content="{{ description or site.description }}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:site_name" content="{{ site.name }}">
  <meta property="og:image" content="{{ site.url }}{% if metadata.media.featuredImage %}{{ metadata.media.featuredImage }}{% else %}/wp-content/uploads/2025/12/think-by-numbers-og.png{% endif %}">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">

  {# Twitter Card #}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:site" content="{{ site.social.twitterHandle }}">
  <meta name="twitter:creator" content="{{ site.social.twitterHandle }}">
  <meta name="twitter:title" content="{% if title %}{{ title | decodeHtml }}{% else %}{{ site.name }}{% endif %}">
  <meta name="twitter:description" content="{{ description or site.description }}">
  <meta name="twitter:image" content="{{ site.url }}{% if metadata.media.featuredImage %}{{ metadata.media.featuredImage }}{% else %}/wp-content/uploads/2025/12/think-by-numbers-twitter.png{% endif %}">

  {# Schema.org structured data #}
  <script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "WebSite",
    "url": "{{ site.url }}",
    "name": "{{ site.name }}",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "{{ site.url }}/search/?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Person",
    "url": "{{ site.url }}",
    "sameAs": [
      "{{ site.social.facebook }}",
      "{{ site.social.linkedin }}",
      "{{ site.social.youtube }}",
      "{{ site.social.twitter }}"
    ],
    "name": "{{ site.author.name }}"
  }
  </script>

  {# RSS Feeds #}
  <link rel="alternate" type="application/atom+xml" title="{{ site.name }} Feed" href="{{ site.feedPath }}">
  <link rel="alternate" type="application/rss+xml" title="{{ site.name }} Podcast Feed" href="{{ site.podcastFeedPath }}">

  <style>
    /* Neobrutalist Design System */
    :root {
      --color-bg: #fafafa;
      --color-black: #000;
      --color-white: #fff;
      --color-primary: #00d9ff;
      --color-secondary: #ffeb3b;
      --color-accent: #ff006e;
      --color-success: #00ff88;
      --border-thick: 4px;
      --shadow-offset: 6px;
    }

    /* Dark Mode Variables */
    [data-theme="dark"] {
      --color-bg: #1a1a1a;
      --color-black: #fff;
      --color-white: #000;
      --color-primary: #00d9ff;
      --color-secondary: #ffeb3b;
      --color-accent: #ff006e;
      --color-success: #00ff88;
    }

    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial Black', 'Arial Bold', sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
      color: var(--color-black);
      background: var(--color-bg);
      font-weight: 700;
    }

    h1, h2, h3, h4, h5, h6 {
      font-weight: 900;
      line-height: 1.2;
    }

    /* Social Header */
    .social-header {
      background: var(--color-black);
      padding: 12px 0;
      border-bottom: var(--border-thick) solid var(--color-black);
    }

    .social-icons {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
      text-align: right;
    }

    .social-icons a {
      margin-left: 20px;
      text-decoration: none;
      color: var(--color-white);
      font-size: 0.85em;
      font-weight: 900;
      text-transform: uppercase;
      transition: color 0.2s;
    }

    .social-icons a:hover {
      color: var(--color-primary);
    }

    /* Main Header */
    .main-header {
      max-width: 1200px;
      margin: 0 auto;
      padding: 30px 20px;
      border-bottom: var(--border-thick) solid var(--color-black);
      background: var(--color-white);
    }

    .main-header p {
      font-size: 1.2em;
      margin: 15px 0;
      font-weight: 900;
    }

    /* Navigation */
    nav {
      margin-top: 20px;
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
    }

    nav a {
      padding: 10px 20px;
      text-decoration: none;
      color: var(--color-black);
      background: var(--color-white);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: var(--shadow-offset) var(--shadow-offset) 0 var(--color-black);
      font-size: 0.9em;
      font-weight: 900;
      text-transform: uppercase;
      transition: all 0.2s;
      display: inline-block;
    }

    nav a:hover {
      transform: translate(3px, 3px);
      box-shadow: 3px 3px 0 var(--color-black);
    }

    nav a.cta-link {
      background: var(--color-primary);
      color: var(--color-black);
    }

    nav a.cta-link:hover {
      background: var(--color-secondary);
    }

    /* Container */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px;
      display: grid;
      grid-template-columns: 1fr 340px;
      gap: 40px;
    }

    .main-content {
      min-width: 0;
    }

    /* Post Grid */
    .post-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 30px;
      margin-top: 30px;
    }

    .post-card {
      border: var(--border-thick) solid var(--color-black);
      background: var(--color-white);
      box-shadow: 8px 8px 0 var(--color-black);
      overflow: hidden;
      transition: all 0.3s;
    }

    .post-card:hover {
      transform: translate(4px, 4px);
      box-shadow: 4px 4px 0 var(--color-black);
    }

    .post-image {
      border-bottom: var(--border-thick) solid var(--color-black);
      background: var(--color-primary);
    }

    .post-image img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      display: block;
      mix-blend-mode: multiply;
    }

    .post-header, .post-info, .post-footer {
      padding: 18px;
    }

    .post-header {
      padding-bottom: 10px;
      background: var(--color-white);
    }

    .post-title {
      margin: 0 0 12px 0;
      font-size: 1.15em;
      line-height: 1.3;
    }

    .post-title a {
      color: var(--color-black);
      text-decoration: none;
      font-weight: 900;
    }

    .post-title a:hover {
      color: var(--color-accent);
    }

    .post-meta {
      color: var(--color-black);
      font-size: 0.8em;
      opacity: 0.7;
      text-transform: uppercase;
    }

    .entry-summary p {
      margin: 0;
      color: var(--color-black);
      font-size: 0.9em;
      font-weight: 500;
      line-height: 1.5;
    }

    .post-footer {
      border-top: var(--border-thick) solid var(--color-black);
      background: var(--color-secondary);
      padding: 12px 18px;
    }

    .post-footer a {
      font-weight: 900;
      text-transform: uppercase;
      font-size: 0.85em;
    }

    .read-more {
      color: var(--color-black);
    }

    /* Sidebar */
    .sidebar {
      position: sticky;
      top: 20px;
      align-self: start;
    }

    .widget {
      background: var(--color-white);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: 8px 8px 0 var(--color-black);
      padding: 25px;
      margin-bottom: 30px;
    }

    .widget h4 {
      margin-top: 0;
      margin-bottom: 18px;
      font-size: 1.3em;
      text-transform: uppercase;
    }

    .widget input[type="text"],
    .widget input[type="email"],
    .widget input[type="search"] {
      width: 100%;
      padding: 12px;
      border: var(--border-thick) solid var(--color-black);
      background: var(--color-white);
      box-sizing: border-box;
      font-family: inherit;
      font-weight: 700;
      font-size: 0.95em;
    }

    .widget input[type="text"]:focus,
    .widget input[type="email"]:focus,
    .widget input[type="search"]:focus {
      outline: none;
      background: var(--color-secondary);
    }

    .widget button {
      width: 100%;
      padding: 12px;
      margin-top: 12px;
      background: var(--color-black);
      color: var(--color-white);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: 4px 4px 0 var(--color-primary);
      cursor: pointer;
      font-family: inherit;
      font-weight: 900;
      font-size: 0.95em;
      text-transform: uppercase;
      transition: all 0.2s;
    }

    .widget button:hover {
      background: var(--color-primary);
      color: var(--color-black);
      transform: translate(2px, 2px);
      box-shadow: 2px 2px 0 var(--color-black);
    }

    .social-links {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .social-links li {
      margin-bottom: 10px;
    }

    .social-links a {
      color: var(--color-black);
      text-decoration: none;
      font-weight: 900;
      padding: 8px 12px;
      background: var(--color-success);
      border: 3px solid var(--color-black);
      display: inline-block;
      transition: all 0.2s;
    }

    .social-links a:hover {
      background: var(--color-accent);
      color: var(--color-white);
    }

    /* Footer */
    footer {
      border-top: var(--border-thick) solid var(--color-black);
      margin-top: 60px;
      padding: 30px 20px;
      text-align: center;
      background: var(--color-black);
      color: var(--color-white);
    }

    footer a {
      color: var(--color-primary);
      text-decoration: none;
      font-weight: 900;
    }

    footer a:hover {
      color: var(--color-secondary);
    }

    /* Article Content */
    article img {
      max-width: 100%;
      height: auto;
      border: var(--border-thick) solid var(--color-black);
    }

    article a {
      color: var(--color-black);
      font-weight: 900;
      text-decoration: underline;
      text-decoration-thickness: 3px;
      text-decoration-color: var(--color-primary);
    }

    article a:hover {
      text-decoration-color: var(--color-accent);
    }

    /* Social Sharing Buttons */
    .social-sharing {
      margin-top: 40px;
      padding-top: 30px;
      border-top: var(--border-thick) solid var(--color-black);
    }

    .social-sharing h3 {
      margin-bottom: 20px;
      font-size: 1.2em;
      text-transform: uppercase;
    }

    .share-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 15px;
    }

    .share-button {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 20px;
      text-decoration: none;
      color: var(--color-black);
      background: var(--color-white);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: 4px 4px 0 var(--color-black);
      font-weight: 900;
      font-size: 0.9em;
      text-transform: uppercase;
      transition: all 0.2s;
      cursor: pointer;
    }

    .share-button:hover {
      transform: translate(2px, 2px);
      box-shadow: 2px 2px 0 var(--color-black);
    }

    .share-button.twitter {
      background: #1DA1F2;
      color: var(--color-white);
    }

    .share-button.twitter:hover {
      background: #0d8bd9;
    }

    .share-button.facebook {
      background: #1877F2;
      color: var(--color-white);
    }

    .share-button.facebook:hover {
      background: #0e66d0;
    }

    .share-button.linkedin {
      background: #0A66C2;
      color: var(--color-white);
    }

    .share-button.linkedin:hover {
      background: #0855a0;
    }

    .share-button.reddit {
      background: #FF4500;
      color: var(--color-white);
    }

    .share-button.reddit:hover {
      background: #e63d00;
    }

    .share-button.email {
      background: var(--color-secondary);
      color: var(--color-black);
    }

    .share-button.email:hover {
      background: var(--color-primary);
    }

    .share-button .icon {
      font-size: 1.1em;
      line-height: 1;
    }

    /* Pagefind Search UI - Neobrutalist Style */
    .pagefind-ui__search-input {
      background: var(--color-white) !important;
      border: var(--border-thick) solid var(--color-black) !important;
      border-radius: 0 !important;
      padding: 15px !important;
      font-family: inherit !important;
      font-weight: 700 !important;
      font-size: 1.1em !important;
      box-shadow: 4px 4px 0 var(--color-black) !important;
    }

    .pagefind-ui__search-input:focus {
      background: var(--color-secondary) !important;
      outline: none !important;
    }

    .pagefind-ui__search-clear {
      background: var(--color-accent) !important;
      color: var(--color-white) !important;
      border: 3px solid var(--color-black) !important;
      border-radius: 0 !important;
      font-weight: 900 !important;
      padding: 8px 12px !important;
    }

    .pagefind-ui__result {
      border: var(--border-thick) solid var(--color-black) !important;
      background: var(--color-white) !important;
      padding: 20px !important;
      margin-bottom: 20px !important;
      box-shadow: 6px 6px 0 var(--color-black) !important;
      border-radius: 0 !important;
    }

    .pagefind-ui__result:hover {
      transform: translate(3px, 3px);
      box-shadow: 3px 3px 0 var(--color-black) !important;
    }

    .pagefind-ui__result-title {
      font-weight: 900 !important;
      color: var(--color-black) !important;
      font-size: 1.3em !important;
    }

    .pagefind-ui__result-link {
      color: var(--color-black) !important;
      text-decoration: none !important;
    }

    .pagefind-ui__result-link:hover {
      color: var(--color-accent) !important;
    }

    .pagefind-ui__result-excerpt {
      color: var(--color-black) !important;
      font-weight: 500 !important;
      line-height: 1.6 !important;
      margin-top: 10px !important;
    }

    .pagefind-ui__result-excerpt mark {
      background: var(--color-secondary) !important;
      color: var(--color-black) !important;
      font-weight: 900 !important;
      padding: 2px 4px !important;
      border: 2px solid var(--color-black) !important;
    }

    .pagefind-ui__button {
      background: var(--color-primary) !important;
      color: var(--color-black) !important;
      border: var(--border-thick) solid var(--color-black) !important;
      border-radius: 0 !important;
      padding: 12px 24px !important;
      font-weight: 900 !important;
      text-transform: uppercase !important;
      box-shadow: 4px 4px 0 var(--color-black) !important;
      cursor: pointer !important;
      transition: all 0.2s !important;
    }

    .pagefind-ui__button:hover {
      background: var(--color-secondary) !important;
      transform: translate(2px, 2px);
      box-shadow: 2px 2px 0 var(--color-black) !important;
    }

    .pagefind-ui__message {
      background: var(--color-white) !important;
      border: var(--border-thick) solid var(--color-black) !important;
      padding: 20px !important;
      font-weight: 700 !important;
      box-shadow: 6px 6px 0 var(--color-black) !important;
      margin-bottom: 20px !important;
    }

    /* Back to Top Button */
    .back-to-top {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      background: var(--color-primary);
      color: var(--color-black);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: 6px 6px 0 var(--color-black);
      cursor: pointer;
      display: none;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      font-weight: 900;
      text-decoration: none;
      transition: all 0.3s;
      z-index: 1000;
      opacity: 0;
      transform: translateY(20px);
    }

    .back-to-top.visible {
      display: flex;
      opacity: 1;
      transform: translateY(0);
    }

    .back-to-top:hover {
      background: var(--color-secondary);
      transform: translate(3px, 3px);
      box-shadow: 3px 3px 0 var(--color-black);
    }

    .back-to-top:active {
      transform: translate(5px, 5px);
      box-shadow: 1px 1px 0 var(--color-black);
    }

    /* Responsive */
    @media (max-width: 768px) {
      :root {
        --shadow-offset: 4px;
      }

      .container {
        grid-template-columns: 1fr;
        padding: 20px 15px;
      }

      .post-grid {
        grid-template-columns: 1fr;
        gap: 25px;
      }

      .sidebar {
        position: static;
      }

      nav {
        gap: 10px;
      }

      nav a {
        font-size: 0.8em;
        padding: 8px 14px;
      }

      .back-to-top {
        bottom: 20px;
        right: 20px;
        width: 50px;
        height: 50px;
        font-size: 20px;
      }

      .share-buttons {
        gap: 8px;
      }

      .share-button {
        font-size: 0.8em;
        padding: 10px 16px;
      }

      .share-button .icon {
        font-size: 1em;
      }
    }

    /* Comments Section */
    .comments-section {
      margin-top: 40px;
      padding-top: 30px;
      border-top: var(--border-thick) solid var(--color-black);
    }

    .comments-section h3 {
      margin-bottom: 20px;
      font-size: 1.2em;
      text-transform: uppercase;
    }

    #disqus_thread {
      background: var(--color-white);
      border: var(--border-thick) solid var(--color-black);
      box-shadow: 6px 6px 0 var(--color-black);
      padding: 30px;
      margin-top: 20px;
    }

    [data-theme="dark"] #disqus_thread {
      background: #2a2a2a;
      border-color: var(--color-black);
    }
  </style>
</head>
<body>
  <header data-pagefind-ignore>
    {# Social Media Header #}
    <div class="social-header">
      <div class="social-icons">
        <a href="{{ site.social.twitter }}" target="_blank" title="Twitter">Twitter</a>
        <a href="{{ site.social.facebook }}" target="_blank" title="Facebook">Facebook</a>
        <a href="{{ site.social.linkedin }}" target="_blank" title="LinkedIn">LinkedIn</a>
        <a href="{{ site.social.youtube }}" target="_blank" title="YouTube">YouTube</a>
        <a href="https://soundcloud.com/plutonium-kidz" target="_blank" title="SoundCloud">SoundCloud</a>
        <a href="{{ site.social.github }}" target="_blank" title="GitHub">GitHub</a>
        <a href="https://mikesinn.com" target="_blank" title="Mike Sinn">Mike Sinn</a>
      </div>
    </div>

    {# Main Header #}
    <div class="main-header">
      <div class="logo">
        <a href="/">
          <img src="{{ site.logo }}" alt="{{ site.name }}" style="max-width: 300px; height: auto;">
        </a>
      </div>
      <p>{{ site.description }}</p>
      <nav>
        <a href="/">Home</a>
        <a href="/empiricism-2/why-think-by-numbers/">Why Think by Numbers?</a>
        <a href="/subscribe/">Subscribe</a>
        <a href="https://warondisease.org" target="_blank" class="cta-link">How to End War and Disease</a>
        <a href="https://dih.earth" target="_blank" class="cta-link">The Decentralized Institutes of Health</a>
        <a href="https://dfda.earth" target="_blank" class="cta-link">The Decentralized FDA</a>
        <a href="https://wishonia.love" target="_blank">Wishonia</a>
        <a href="/search/" class="search-link">Search</a>
      </nav>
    </div>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <p>&copy; 2025 <a href="https://mikesinn.com">Mike P. Sinn</a> | <a rel="license" href="http://creativecommons.org/licenses/by-nc/4.0/">CC BY-NC 4.0</a></p>
    <p>
      <a href="{{ site.feedPath }}">RSS Feed</a> |
      <a href="{{ site.podcastFeedPath }}">Podcast Feed</a> |
      <a href="/search/">Search</a> |
      <a href="https://mikesinn.com/privacy-policy" target="_blank">Privacy Policy</a>
    </p>
  </footer>

  {# Pagefind Search #}
  <link href="/_pagefind/pagefind-ui.css" rel="stylesheet">
  <script src="/_pagefind/pagefind-ui.js"></script>

  {# Back to Top Button #}
  <a href="#" class="back-to-top" id="back-to-top" aria-label="Back to top" title="Back to top">↑</a>

  {# KaTeX for LaTeX rendering #}
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
  <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
  <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js" onload="renderMathInElement(document.body, {
    delimiters: [
      {left: '$$', right: '$$', display: true},
      {left: '$', right: '$', display: false},
      {left: '\\[', right: '\\]', display: true},
      {left: '\\(', right: '\\)', display: false}
    ]
  });"></script>

  {# Back to Top Button Script #}
  <script>
    (function() {
      const backToTopButton = document.getElementById('back-to-top');
      const scrollThreshold = 300; // Show button after scrolling 300px

      function toggleBackToTop() {
        if (window.pageYOffset > scrollThreshold) {
          backToTopButton.classList.add('visible');
        } else {
          backToTopButton.classList.remove('visible');
        }
      }

      // Show/hide button based on scroll position
      window.addEventListener('scroll', toggleBackToTop);

      // Smooth scroll to top when clicked
      backToTopButton.addEventListener('click', function(e) {
        e.preventDefault();
        window.scrollTo({
          top: 0,
          behavior: 'smooth'
        });
      });

      // Check initial scroll position
      toggleBackToTop();
    })();
  </script>

  {# Disqus Comments Script #}
  {%- if site.disqusShortname and title %}
  <script>
    var disqus_config = function () {
      this.page.url = '{{ site.url }}{{ page.url }}';
      this.page.identifier = '{{ page.url }}';
      this.page.title = '{{ title | decodeHtml | escape }}';
    };
    (function() {
      var d = document, s = d.createElement('script');
      s.src = 'https://{{ site.disqusShortname }}.disqus.com/embed.js';
      s.setAttribute('data-timestamp', +new Date());
      (d.head || d.body).appendChild(s);
    })();
  </script>
  {%- endif %}
</body>
</html>
