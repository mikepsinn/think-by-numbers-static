<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} - {% endif %}{{ site.name }}</title>
  <meta name="description" content="{{ description or site.description }}">

  {# RSS Feeds #}
  <link rel="alternate" type="application/atom+xml" title="{{ site.name }} Feed" href="{{ site.feedPath }}">
  <link rel="alternate" type="application/rss+xml" title="{{ site.name }} Podcast Feed" href="{{ site.podcastFeedPath }}">

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      color: #333;
    }
    header {
      border-bottom: 2px solid #333;
      padding-bottom: 20px;
      margin-bottom: 40px;
    }
    h1 { margin: 0; }
    nav a {
      margin-right: 20px;
      text-decoration: none;
      color: #0066cc;
    }
    footer {
      border-top: 1px solid #ddd;
      margin-top: 60px;
      padding-top: 20px;
      text-align: center;
      color: #666;
    }
    article img {
      max-width: 100%;
      height: auto;
    }
  </style>
</head>
<body>
  <header>
    <h1><a href="/" style="text-decoration: none; color: inherit;">{{ site.name }}</a></h1>
    <p>{{ site.description }}</p>
    <nav>
      <a href="/">Home</a>
      <a href="{{ site.feedPath }}">RSS</a>
      <a href="{{ site.podcastFeedPath }}">Podcast</a>
      <a href="/search/">Search</a>
    </nav>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <p>&copy; {{ site.name }} | <a href="/privacy-policy/">Privacy Policy</a></p>
    <p>
      <a href="{{ site.feedPath }}">RSS Feed</a> |
      <a href="{{ site.podcastFeedPath }}">Podcast Feed</a>
    </p>
  </footer>

  {# Pagefind Search #}
  <link href="/_pagefind/pagefind-ui.css" rel="stylesheet">
  <script src="/_pagefind/pagefind-ui.js"></script>
</body>
</html>
