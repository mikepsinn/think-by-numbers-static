---
layout: base.njk
---

<div class="container">
  <div class="main-content">
    {%- if metadata.media.featuredImage %}
    <div class="featured-image" style="margin-bottom: 30px;">
      <img src="{{ metadata.media.featuredImage }}" alt="{{ title | decodeHtml }}" style="max-width: 100%; height: auto; border-radius: 4px;">
    </div>
    {%- endif %}

    <article>
      <h1>{{ title | decodeHtml }}</h1>
      <time datetime="{{ date | dateToISO }}" style="color: #666; font-size: 0.9em;">{{ date | readableDate }}</time>

      {%- if metadata.categories %}
      <p style="margin-top: 10px;"><strong>Categories:</strong>
        {%- for category in metadata.categories %}
          {{ category }}{% if not loop.last %}, {% endif %}
        {%- endfor %}
      </p>
      {%- endif %}

      <div class="post-content" style="margin-top: 30px;">
        {{ content | safe }}
      </div>
    </article>
  </div>

  <aside class="sidebar">
    <div class="widget">
      <h4>Search</h4>
      <form action="/search/" method="get">
        <input type="text" name="q" placeholder="Search posts..." required>
        <button type="submit">🔍 Search</button>
      </form>
    </div>

    <div class="widget">
      <h4>Follow Us</h4>
      <ul class="social-links">
        <li><a href="{{ site.social.facebook }}" target="_blank">Facebook</a></li>
        <li><a href="{{ site.social.twitter }}" target="_blank">Twitter</a></li>
        <li><a href="{{ site.social.linkedin }}" target="_blank">LinkedIn</a></li>
      </ul>
    </div>

    <div class="widget">
      <h4>Subscribe</h4>
      <p>Join thousands of subscribers</p>
      <form action="#">
        <input type="email" placeholder="Your email">
        <button type="submit">Subscribe</button>
      </form>
    </div>
  </aside>
</div>
